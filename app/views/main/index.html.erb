<div class="main-content">
  <% if user_signed_in? %>
    <div class="graph">
      <% if @fdata.present? %>
        <h1>あなたのPFCバランス</h1>
        <%= pie_chart @fdata, height: "150px" %>
        <%= link_to "PFCバランスとは？", "https://rariowa.com", class: 'blog-link' %>
      <% end %>
    </div>
    <div class="weight-graph">
      <h1>体重推移</h1>
      <%# <div class="chart-container" style="position: relative; height:150px; width:300px">
        <canvas  id="myChart"></canvas>
      </div>
      <script>draw_graph();</script> %>
      <%= line_chart Weight.pluck(:date, :weight), height: "200px" %>
    </div>
  <% else %>
    <div class="main-content">
      <h2>
        <p>本気で痩せる人のための食事管理アプリ</p>
      </h2>
    </div>
  <% end %>
</div>
